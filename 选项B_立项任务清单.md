# 选项 B：基座姿态等变逆动力学 —— 立项任务清单

> 配套文件：`选项B_基座姿态等变_查重与定位.md`（查重已通过，见该文件）。
> 本清单按"先验生死门、再上量"排，每阶段有**判据**，判据不过就停下，别硬冲。

---

## 0. 先认清 novelty 到底是什么（防自嗨，写进论文的第一句）

**你的贡献不是**：
- 不是"发现重力线性于重力方向"——这是经典刚体力学教科书（牛顿-欧拉/RNE）的常识；
- 不是"提出等变学习范式"——Duong 占了"李群+结构保持动力学学习"（做自由浮体/飞行器），Seo 占了"SE(3) 等变 + 操作/控制"，上游位子都被占了。

**你的贡献是**：
> 把经典"基座旋转等变"做成**学习逆动力学模型的结构约束**，做到
> **"单姿态训练 → 全安装姿态零重训"**，并给出 vs 黑箱模型的干净对照 + 真机故事。

一句话卖点：**"黑箱模型把重力学进了权重，换安装姿态就得重训；我们的结构化模型把重力方向做成显式输入，换姿态零重训。"**

---

## 1. 最小可复现实验（go/no-go 门，2–4 周，先做这个）

**这是决定整个项目值不值得投的生死门。跑通之前，别做后面任何一步。**

### 生死门的对照：不是两组，是四组

| 组 | 训练数据 | 换姿态后零重训 |
|---|---|---|
| ① 结构模型（g 对 g_dir 构造性线性） | 仅水平 | 应该保持 ✅ |
| ② 裸黑箱 MLP（不喂重力方向） | 仅水平 | 应该失效（sanity check） |
| ③ **带 g_dir 输入的黑箱 MLP** | 仅水平 | **？← 真正的对手** |
| ④ 带 g_dir 输入的黑箱 MLP | **多姿态** | 留出姿态（审稿人必问） |

**第三组才是生死门的承重墙。** ②赢③是"不公平的赢"——审稿人一句"你给黑箱也喂个重力方向输入，它也能学"就把你打回。真正的卖点是：**黑箱即使拿到 g_dir 输入、零重训，还是明显差**，因为它没被约束"g 对 g_dir 必须线性"，学的是非线性近似；而结构模型的线性约束让零重训**构造性地成立**。

### 第三组怎么造（写死，别含糊）

- 输入 = z-score 后的 [q, q̇, q̈] ⊕ ĝ（**单位**重力方向，21+3=24 维）。
- ĝ 取**单位向量**而不是 (0,0,−9.81)：所有安装姿态的重力**模长都等于 9.81**，模长不携带任何"换姿态"信息，只有方向携带；喂单位向量既信息无损，又避开 9.81 与 z-score 输入的尺度失配。
- 网络架构、隐层、训练超参与裸黑箱②**逐位相同**，唯一差别是多 3 维 ĝ 输入。
- 训练数据与①②**完全相同**（只有基座水平数据）——这才是零重训的公平对照。

**为什么③是真对手（一句话）**：③和①拿到的是**同样的信息**（q,q̇,q̈ + 重力方向），差别只在**结构**——①把 g 约束成 g_dir 的线性函数，③没有。③泛化得好坏，直接隔离出"线性结构约束"到底值不值钱。

### 第四组：审稿人必问，提前堵死

审稿人几乎必然反击："③只在单一姿态训练，g_dir 从没变过，黑箱当然学不到 g_dir 的映射；你应该给黑箱喂**多姿态**数据再测。"

→ ④ 就用多姿态训练黑箱（g_dir 在训练里变化），测一个**留出的姿态**：
- 若④仍明显差 → 结构约束价值拉满：黑箱给足数据都学不好"g 对 g_dir 的线性"，结构等变是**样本效率 + 保证性**的双重优势。
- 若④追平 → 故事降级为"结构 = 数据效率 / 构造性保证 vs 黑箱 = 需多姿态数据"，仍可写但卖点变弱。**这个结果决定论文定位档位，必须早测。**

### 判据（①③两条都成立才通过）

- ✅ **结构模型①**在倾斜姿态上误差**不涨**（零重训成功）；
- ✅ **黑箱③**（喂了 g_dir）在倾斜姿态上误差**明显涨**（证明线性结构约束值钱，而非"喂个输入谁都会"）。
- ②裸黑箱失效只是 sanity check，**不构成卖点**；④定档位。

**任何一条不成立 → 停下重想。** 特别是：若③零重训其实没差太多，那"结构约束"这个卖点站不住，整个项目要重新评估。

---

## 2. 方法设计（2–3 周）

### 核心改动：把重力项 g 从"任意网络"改成"显式线性于重力方向"

现在的 DeLaN 结构里，重力项是 `g(q)=∂V/∂q`，V(q) 是个任意网络——**它把重力方向烧进了权重，所以换基座姿态就废了**。

改成：

> `V(q; g_dir) = g_dirᵀ · p(q)`，其中 `p(q) ∈ R³` 是学的"势能方向"，`g_dir` 是基座系里的重力方向（3 维向量）。

于是 `g = ∂V/∂q` **线性于 g_dir**，换安装姿态 = 换 g_dir = **模型自动对，零重训**。等变是构造性的。

### 决策点：p(q) 从哪来？
- ❌ **不要**从 M=ΣJᵀGJ 学的 G_i（质量×质心）推——上一轮 θ̂ 报告已经证明学回来的物理参数（m、质心）是**垃圾**（在零空间乱飘），从垃圾 m_i 推 g 会把等变也带崩。
- ✅ **直接学 p(q)**：单独一个小网络/参数化，学"可辨识的势能方向"，避开垃圾物理参数。

### 复用（技术增量小的原因）
- **M 项**：M(q)=ΣJᵀGJ 全部复用（M 是基座旋转不变的，不用改）；
- **c 项**：JVP 科里奥利全部复用（c 也是基座旋转不变的）；
- **只改 g 项**：V(q) 网络换成 V(q; g_dir)=g_dirᵀ p(q)。

---

## 3. 实验矩阵（3–4 周）

- **多姿态**：地面 / 墙面 / 倒挂 / 倾斜 30°、45°，**单姿态训练 → 全姿态测试**。
- **对照**：黑箱 MLP、DeLaN-FFNN、经典 RNE 辨识（已知基座方向，但换姿态需重新辨识）。
- **指标**：力矩 R²、重力补偿误差、闭环跟踪误差（全部跨姿态报）。

---

## 4. 真机 / 半真机故事（3–4 周，96 分最大杠杆）

- **第一时间确认**：手里有没有能"换安装方向"的真机（装到不同工位 / 移动底盘 / 倾斜台）。
- 能 → 真机数据，96 概率大涨；不能 → 退半真机（MuJoCo 高保真 + 一个真实重装/倾斜场景），并明确写"高保真仿真 + 真机待验证"。
- 真机数据是这个方向冲二区的**最大单一杠杆**，越早确认越好，别拖到最后。

---

## 5. 写作 + 差异化（2–3 周）

**必引、必区分的三篇**（查重时已经圈出来）：
1. **Duong & Atanasov 三篇**（RSS2021/2022/2024）：前向 Hamiltonian/port-Hamiltonian ODE，摆/刚体/四旋翼，**非逆动力学、非串链臂**。
2. **Safe Whole-Body Loco-Manipulation（arXiv 2603.02443）**：处理了基座旋转 + 重力重定向，但是 **RL 把重力当观测喂进去（黑箱适应）**，不是"构造性等变 + 零重训"。这是你**最需要明确区分**的一篇。
3. **Seo et al. 2025 教程综述**：SE(3) 等变机器人学习的大框架，定位你的工作在里面的位置。

**划边界（写 Related Work 时照抄这句口径）**：
> Duong 占的是「李群 + 结构保持动力学学习」（但做自由浮体 / 飞行器，非串链臂逆动力学）；
> Seo 占的是「SE(3) 等变 + 操作 / 控制」。本工作的增量是 **「等变 × 逆动力学 × 串链臂 × 基座姿态」**
> 这个应用级组合——四个坐标轴的交点，单独看每一维都有人碰过，四维同框没人做。

**贡献三条**：
1. 基座姿态等变的逆动力学学习结构（g_dir 显式输入，构造性等变）；
2. "单姿态训练 → 全姿态零重训"的实验证据（vs 黑箱失效）；
3. 真机/半真机故事（移动底盘 / 多工位重装）。

**诚实边界**：novelty 是应用级，不是范式级；重力线性于重力方向是经典性质，本工作的增量在"把它做成学习模型的结构 + 零重训的工程验证"。

---

## 三个诚实风险（先认账，别事后被审稿人打脸）

1. **"重力线性于重力方向"是教科书**，审稿人可能说 trivial。→ 应对：重心放在"黑箱 vs 结构"的零重训对照 + 真机，**不吹"发现等变"**。
2. **最小实验可能"不显然"**：如果黑箱在倾斜姿态上其实没差太多（重力慢变），优势不明显。→ 这是 §1 生死门要验的第一件事，过了才往下投。
3. **垃圾 m_i 的坑**：别从 G_i 推 g，直接学 p(q)，否则等变被垃圾物理参数带崩。
